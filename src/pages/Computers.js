import ProductDisplay from "../components/ProductDisplay"

function Computers(props) {
    return (
        <h1>Computers</h1>

        //idea here is to add a conditional where if the product description includes Computer, display the item on this page
    )
}

export default Computers